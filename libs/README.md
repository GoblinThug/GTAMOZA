# libs/

Place `ScriptHookVDotNet3.dll` here if GTA Enhanced is not installed locally.

CI downloads the latest SHVDN nightly via `scripts/fetch-shvdn.ps1`.

This folder is gitignored (except this README).
